识汉字使用了此控件，要求bin文件支持。
```
src = file:... 1_0_9.bin|鸟.ght

标准写法：
<hzstroke class="gif_img" :src="tabItem.gif" cache="true" repeat="true" stroke_color="000000" cur_stroke_color="FF0000"/>
.gif_img {
    .g_container(142, 142, #ffffff, 10, 0);
}
```

| 属性名称 | 描述 | 值类别 | 例子 | lvgl对应方法 |
|:-----|:----|:----|----|:----|
| src | 汉字描红源 | 字符串，支持 本地文件，URL和base64三种格式  |```<hzstroke src=”http://abc.com/123.ght” /><hzstroke src=“file:/123.ght” /><hzstroke src=“data:image/ght;base64,iVBORw0KGgoAAAA...SAAAAABJRU5ErkJggg==” />``` |lv_hzstroke_set_src|
| cache | 图片是否要缓存 | bool |```<hzstroke cache="true" /> ``` |无|
| repeat | 是否循环播放 | bool |```<hzstroke :repeat="true" /> ``` |lv_hzstroke_set_repeat|
| status | 描红动画状态控制| int (停止0，开始1，暂停2，恢复3，重置4 |```<hzstroke :status="1" /> ``` |lv_hzstroke_set_status|
| speed | 描红的速度，毫秒，每一步的时间差，默认200| int |```<hzstroke :speed="200" /> ``` |lv_hzstroke_set_status|
| line_color | 米字格颜色| color |```<hzstroke :line_color="#ff0000" /> ``` |lv_hzstroke_set_line_color|
| stroke_bg_color | 描红的背景色| color |```<hzstroke :stroke_bg_color="#ff0000" /> ``` |lv_hzstroke_set_stroke_bg_color|
| stroke_color | 描红的| color |```<hzstroke :stroke_color="ff0000" /> ``` |lv_hzstroke_set_stroke_color|
| cur_stroke_color | 描红的当前笔的颜色| color |```<hzstroke cur_stroke_color="ff0000" /> ``` |lv_hzstroke_set_stroke_color|

![图片](../../image/ef03a96e-3f7f-45eb-a2c8-70a9223a8ca0.png)

图片src支持以下三种格式
* 本地文件

```<hzstroke src=“file:/123.ght” />```
必须以file:开头，APP运行目录为跟路径

* http的URL

```<hzstroke src=”http://abc.com/123.ght” />```
URL地址，不推荐使用该方法，会造成设备端太多的http请求线程
如果页面只是一个大文件，可以使用这种方式

对于http格式，可以通过在dom添加 img_http_status_changed事件，获取图片下载的状态分别是
start，success，failed
例子：
```
layout.xml
    <hzstroke :src="img_path" 
         @img_http_status_changed="img_changed() @ready="ready_cb()"/>     

script.js

    img_changed:function(status){
        console.log("image_changed status", status)
    }
    ready_cb:function(){
        console.log("ready_cb", status)
    }

```
* base64
服务器返回图片，建议使用data:base64格式，减少http请求，但建议每个文件小于30k
```<hzstroke src=“data:image/ght;base64,iVBORw0KGgoAAAA...SAAAAABJRU5ErkJggg==” />```